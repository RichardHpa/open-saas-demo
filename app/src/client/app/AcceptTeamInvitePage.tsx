import { Dialog, Box, Container, Typography, Button } from '@mui/material';

const AcceptTeamInvitePage = () => {
  return (
    <Container component='main' maxWidth='md'>
      {/* <Box
          sx={{
            height: '100%',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          <Typography variant='h3'>You have been invited to join</Typography>
          <Button variant='contained'>Accept Invite</Button>
        </Box> */}
    </Container>
  );
};

export default AcceptTeamInvitePage;
